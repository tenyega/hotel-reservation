<form method="post" action="{{ path('admin_deleteRoom', {'id': room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input
	type="hidden" name="_token" value="{{ csrf_token('delete' ~ room.id) }}">
	{# <button class="btn">Supprimer</button> #}
	<a href="{{ path('admin_deleteRoom', {'id': room.id}) }}">
		<input class='btn-reserver' type="button" value="Supprimer">
	</a>
</form>
